# Universal Create Items v1.3

Changes from v1.2:
- Default Location is now `RM0152` (Unassigned Room) on initial load.
- Location remains searchable and can be changed normally.

Expected:
- Location table contains a `LocationID` value exactly equal to `RM0152`.
